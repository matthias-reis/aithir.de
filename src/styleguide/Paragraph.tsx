import styled from '@emotion/styled';
import { colorHighlight } from './var';

export const PLead = styled.p`
  font-size: 1.2rem;
`;

export const PMeta = styled.p`
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: #fffb;
  margin: 0;
`;
