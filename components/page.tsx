import styled from '@emotion/styled';
import { COLOR_BG } from './_styles';

export const Page = styled.div`
  max-width: 50rem;
  padding: 1rem;
  margin: 0 auto;
  background: ${COLOR_BG};
  border-radius: 2rem;
`;
